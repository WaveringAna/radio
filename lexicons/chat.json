{
  "lexicon": 1,
  "id": "pet.nkp.radio.chat",
  "description": "Chat message records for an nkp radio room.",
  "defs": {
    "main": {
      "type": "record",
      "description": "A single chat message attached to a radio host.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["text", "radio", "createdAt"],
        "properties": {
          "text": {
            "type": "string",
            "maxLength": 3000,
            "maxGraphemes": 500,
            "description": "Message body."
          },
          "radio": {
            "type": "string",
            "maxLength": 253,
            "description": "Radio hostname this message belongs to, such as radio.wisp.place."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "Client-declared timestamp when the message was created."
          }
        }
      }
    }
  }
}
